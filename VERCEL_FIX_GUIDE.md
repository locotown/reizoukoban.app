# 🚨 Vercel設定修正ガイド - /app/ 404エラー解決

## 現在の問題

### エラー内容
- `/app/` にアクセスすると404エラー
- `/app/flutter_bootstrap.js` が404
- `/app/manifest.json` が404
- `/app/icons/icon-192.png` が404

### 根本原因
**Vercel Dashboard の `Output Directory` 設定が `public` になっていない**

---

## ✅ 解決手順（画像付き推奨）

### Step 1: Vercel Dashboard にログイン
https://vercel.com/dashboard

### Step 2: プロジェクト選択
- プロジェクト一覧から **`reizoukoban-app`** をクリック

### Step 3: Settings タブに移動
- 画面上部の **Settings** タブをクリック

### Step 4: Build & Development Settings を開く
- 左サイドバーから **Build & Development Settings** をクリック

### Step 5: Output Directory を確認・変更

**現在の設定（推測）:**
```
Output Directory: build/web  ❌ または 空欄
```

**正しい設定:**
```
Output Directory: public  ✅
```

**変更手順:**
1. `Output Directory` の横にある **Override** をONにする
2. テキストボックスに `public` と入力
3. **Save** ボタンをクリック

### Step 6: その他の設定確認

| 設定項目 | 正しい値 |
|---------|---------|
| Framework Preset | `Other` |
| Root Directory | 空欄 |
| Build Command | 空欄 (Override OFF) |
| **Output Directory** | **`public`** ✅ |
| Install Command | 空欄 (Override OFF) |
| Development Command | `None` |

### Step 7: 再デプロイ
1. 画面上部の **Deployments** タブに移動
2. 最新のデプロイ（コミット `2b07e69`）をクリック
3. 右上の **︙**（三点リーダー）をクリック
4. **Redeploy** を選択
5. 確認ダイアログで **Redeploy** をクリック
6. 約1-2分待つ

---

## 🧪 デプロイ完了後の確認

### ランディングページ確認
```
https://reizoukoban-app.vercel.app/
```

**✅ 期待される結果:**
- プロフェッショナルな新デザインが表示される
- ヒーローセクションが2カラムで表示される（PC）
- すべてのセクションが正しく配置されている
- CSS/JSが正しく読み込まれている

### Flutterアプリ確認
```
https://reizoukoban-app.vercel.app/app/
```

**✅ 期待される結果:**
- ログイン画面が表示される（404エラーなし）
- ブラウザコンソール（F12）にエラーがない
- 以下のファイルが200 OKで読み込まれる:
  - `flutter_bootstrap.js`
  - `manifest.json`
  - `icons/Icon-192.png`
  - `main.dart.js`

### デモモード確認
```
https://reizoukoban-app.vercel.app/app/?demo=true
```

**✅ 期待される結果:**
- 自動的に匿名ログインが実行される
- ダッシュボード画面が表示される

---

## 🐛 まだ404エラーが出る場合

### チェックリスト
- [ ] `Output Directory` が `public` に設定されている
- [ ] 最新のコミット（`2b07e69`）がデプロイされている
- [ ] 再デプロイが完了している（約1-2分）
- [ ] ブラウザキャッシュをクリアしている（Ctrl+Shift+R / Cmd+Shift+R）

### デバッグ手順

#### 1. Vercel デプロイログを確認
1. Deployments タブで最新のデプロイをクリック
2. **Build Logs** を確認
3. エラーメッセージがないか確認

#### 2. ブラウザコンソールを確認
1. `/app/` にアクセス
2. F12キーでデベロッパーツールを開く
3. **Console** タブを確認
4. **Network** タブで404エラーのURLを確認

#### 3. 直接URLアクセステスト
以下のURLに直接アクセスして200 OKが返るか確認:
- `https://reizoukoban-app.vercel.app/app/flutter_bootstrap.js`
- `https://reizoukoban-app.vercel.app/app/manifest.json`
- `https://reizoukoban-app.vercel.app/app/icons/Icon-192.png`

**404が返る場合:**
- `Output Directory` 設定が反映されていない
- 再度、Settings → Build & Development Settings → Output Directory: `public` → Save → Redeploy

---

## 📊 プロジェクト構造（確認用）

### GitHub リポジトリ構造
```
/
├── public/                 ← Vercelが配信すべきディレクトリ
│   ├── index.html         ← /
│   ├── css/
│   ├── js/
│   ├── assets/
│   └── app/               ← /app/
│       ├── index.html
│       ├── flutter_bootstrap.js
│       ├── main.dart.js
│       └── ...
├── vercel.json
└── README.md
```

### Vercel が配信するURL構造
```
/ → public/index.html
/css/* → public/css/*
/js/* → public/js/*
/assets/* → public/assets/*
/app/ → public/app/index.html
/app/flutter_bootstrap.js → public/app/flutter_bootstrap.js
```

---

## 🎯 まとめ

### 問題の原因
**Vercel Dashboard の `Output Directory` 設定が `public` になっていない**

### 解決方法
1. Vercel Dashboard → Settings → Build & Development Settings
2. `Output Directory` を `public` に設定
3. Save → Redeploy
4. 1-2分待機
5. 確認

### 完了チェック
- [ ] Vercel設定で `Output Directory: public` を確認
- [ ] 再デプロイ完了
- [ ] ランディングページが新デザインで表示される
- [ ] `/app/` でログイン画面が表示される（404なし）
- [ ] ブラウザコンソールにエラーがない

---

**設定完了後、スクリーンショットを共有していただけると、問題解決の確認ができます。**

最終更新: 2024-12-03  
コミット: `2b07e69` - プロフェッショナルUIリデザイン
