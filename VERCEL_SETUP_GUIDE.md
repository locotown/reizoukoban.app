# 🚀 Vercel設定完全ガイド - 最新版 (public/ディレクトリ方式)

## ⚠️ 重要: Vercel Dashboard での設定変更が必須

コミット `78ff66f` でプロジェクト構造を完全に変更しました。  
Vercel Dashboard で以下の設定を行う必要があります。

---

## 📁 新しいプロジェクト構造

```
public/                  ← Vercelが配信するルートディレクトリ
├── index.html          ← ランディングページ (/)
├── css/
│   └── style.css       ← /css/style.css
├── js/
│   └── main.js         ← /js/main.js
├── assets/
│   ├── favicon.png
│   ├── logo.png
│   └── images/         ← /assets/images/*
└── app/                ← Flutterアプリ (/app/)
    ├── index.html      ← /app/ または /app/index.html
    ├── flutter_bootstrap.js  ← /app/flutter_bootstrap.js
    ├── main.dart.js    ← /app/main.dart.js
    ├── manifest.json   ← /app/manifest.json
    ├── icons/          ← /app/icons/*
    ├── assets/         ← /app/assets/*
    └── canvaskit/      ← /app/canvaskit/*
```

---

## ✅ Vercel Dashboard 設定手順

### 1. Vercel Dashboard にアクセス
https://vercel.com/dashboard

### 2. プロジェクト `reizoukoban-app` を選択

### 3. **Settings** → **Build & Development Settings** に移動

### 4. 以下の設定を行う:

| 設定項目                | 設定値                     | 説明                          |
|-------------------------|----------------------------|-------------------------------|
| **Framework Preset**    | `Other`                   | カスタム設定                  |
| **Root Directory**      | **空欄** または `.`        | プロジェクトルート            |
| **Build Command**       | **空欄** (Override OFF)    | ビルド不要                    |
| **Output Directory**    | `public`                  | ⚠️ **最重要!** `public` を指定 |
| **Install Command**     | **空欄** (Override OFF)    | インストール不要              |
| **Development Command** | `None`                    | 開発コマンドなし              |

### 5. **Save** ボタンをクリック

### 6. **Deployments** タブに戻る

### 7. **Redeploy** ボタンをクリック (最新のコミット `78ff66f` を再デプロイ)

---

## 🎯 期待される動作

### ランディングページ (/)
- **URL**: `https://reizoukoban-app.vercel.app/`
- **ファイル**: `public/index.html`
- **CSS**: `public/css/style.css` → `/css/style.css`
- **JS**: `public/js/main.js` → `/js/main.js`
- **画像**: `public/assets/images/*` → `/assets/images/*`

### Flutterアプリ (/app/)
- **URL**: `https://reizoukoban-app.vercel.app/app/`
- **ファイル**: `public/app/index.html`
- **Flutter JS**: `public/app/flutter_bootstrap.js` → `/app/flutter_bootstrap.js`
- **メインコード**: `public/app/main.dart.js` → `/app/main.dart.js`
- **アイコン**: `public/app/icons/Icon-192.png` → `/app/icons/Icon-192.png`
- **マニフェスト**: `public/app/manifest.json` → `/app/manifest.json`

---

## 🔧 vercel.json の役割

現在の `vercel.json` は非常にシンプルです:

```json
{
  "version": 2,
  "cleanUrls": true,
  "trailingSlash": false,
  "headers": [...]
}
```

**ルーティング設定は不要**:
- Vercelが `Output Directory: public` を認識すれば、自動的に `public/` をルートとして配信します
- `/` → `public/index.html`
- `/app/` → `public/app/index.html`
- `/css/style.css` → `public/css/style.css`

---

## 🐛 トラブルシューティング

### 問題1: ランディングページが404
**原因**: `Output Directory` が設定されていない  
**解決策**: Vercel設定で `Output Directory: public` を指定

### 問題2: `/app/` が404
**原因**: 同上  
**解決策**: 同上 + 再デプロイ

### 問題3: CSS/JSが404
**原因**: 同上  
**解決策**: 同上 + ブラウザキャッシュクリア

### 問題4: Flutter静的ファイルが404
**原因**: `public/app/` 配下のファイルが正しく配信されていない  
**解決策**: 
1. `Output Directory: public` を設定
2. 再デプロイ
3. ブラウザコンソール (F12) で実際のURLを確認

---

## ✅ デプロイ確認チェックリスト

### Vercel設定確認
- [ ] `Output Directory` が `public` に設定されている
- [ ] 最新のコミット `78ff66f` がデプロイされている
- [ ] デプロイステータスが **Ready** になっている

### ランディングページ確認
- [ ] `https://reizoukoban-app.vercel.app/` にアクセス
- [ ] ヒーローセクションが表示される
- [ ] CSS/JSが正しく読み込まれる (スタイルが適用されている)
- [ ] ファビコンが表示される
- [ ] 「無料で始める」ボタンが機能する
- [ ] 「デモを試す」ボタンが機能する

### Flutterアプリ確認
- [ ] `https://reizoukoban-app.vercel.app/app/` にアクセス
- [ ] ログイン画面が表示される (404エラーなし)
- [ ] ブラウザコンソール (F12) にエラーがない
- [ ] `flutter_bootstrap.js` が 200 OK で読み込まれる
- [ ] `manifest.json` が 200 OK で読み込まれる
- [ ] `icons/Icon-192.png` が 200 OK で読み込まれる
- [ ] デモモード (`/app/?demo=true`) で自動ログインが実行される

---

## 📊 プロジェクト状態

- ✅ **public/ ディレクトリ作成完了**
- ✅ **vercel.json 簡素化完了**
- ✅ **GitHubプッシュ完了** (コミット: `78ff66f`)
- ⏳ **Vercel設定変更待ち** (`Output Directory: public`)
- ⏳ **再デプロイ待ち**

---

## 🚨 次のアクション (ユーザー側)

1. **Vercel Dashboard にアクセス**
2. **Settings → Build & Development Settings**
3. **`Output Directory` を `public` に設定**
4. **Save → Redeploy**
5. **デプロイ完了を待つ (約1-2分)**
6. **ランディングページとアプリページの動作を確認**

**設定完了後、動作結果を教えてください！**

---

最終更新: 2024-12-03  
コミット: `78ff66f` - public/ディレクトリ方式への完全移行
